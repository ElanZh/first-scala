object Hello extends App {
  val str: String = "Hello, World!"
  println(str)
  println(str(3))

  math.Pi
  math.random()

  def max(x:Int,y:Int):Int={
    if(x>y) x
    else y
  }
  def +(x: Int):Int = {return 4}
}